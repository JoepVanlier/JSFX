@init

function init_vowels(freemem)
(
  vowel = freemem;

  handle = file_open("partials_dependencies/saike_vowel_data.ske");
  file_mem(handle, freemem, 22050 * 8);
  file_close(handle);
  
  freemem;
);

